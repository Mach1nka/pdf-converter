import FormWrapper from '../../components/authorization/FormWrapper';
import LoginForm from '../../components/authorization/LoginForm';

const Login: React.FC = () => {
  return (
    <FormWrapper>
      <LoginForm />
    </FormWrapper>
  );
};

export default Login;
