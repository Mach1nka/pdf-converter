// import { Theme } from '@mui/material';
// import { StylesOverride } from '../types/types';

// const useStyles = (styles: StylesOverride, theme?: Theme): StylesOverride => styles;

// export default useStyles;
